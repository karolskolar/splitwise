version: '3.8'

services:
  api:
    build: .
    container_name: splitwise-api
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - CORS_ORIGIN=*
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - JWT_SECRET=${JWT_SECRET}
      # For production, set specific origin:
      # - CORS_ORIGIN=https://yourusername.github.io
    volumes:
      - ./data:/app/data
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3001/health"]
      interval: 30s
      timeout: 3s
      retries: 3

# Optional: Add Caddy for automatic HTTPS
# Uncomment below if you want automatic SSL with your domain

#  caddy:
#    image: caddy:2-alpine
#    container_name: splitwise-caddy
#    restart: unless-stopped
#    ports:
#      - "80:80"
#      - "443:443"
#    volumes:
#      - ./Caddyfile:/etc/caddy/Caddyfile:ro
#      - caddy_data:/data
#      - caddy_config:/config
#    depends_on:
#      - api

#volumes:
#  caddy_data:
#  caddy_config:
